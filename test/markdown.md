# Alchemos v1

## Theme Test

This is **bold text**.

This is *italic text*.

This is ***bold italic text***.

> This is a quote.

---

## Inline Code

Use `const player = new Player()` here.

## Code Block

```javascript
const player = {
    name: "Alchemos",
    level: 9000,
    active: true
};

function attack(target) {
    return target.health - 25;
}